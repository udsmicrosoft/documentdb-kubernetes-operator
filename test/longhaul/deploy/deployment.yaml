# Copyright (c) Microsoft Corporation.
# Licensed under the MIT License.

# Long Haul Test - Kubernetes Deployment
#
# Why a Deployment (not a Job)?
#   A Job's pod spec is immutable, so rolling a new image requires
#   delete-and-recreate. Using Deployment(replicas=1) lets the deploy
#   workflow do `kubectl set image deployment/longhaul-test ...` for
#   one-line image rolls. The driver itself is restart-safe: it resumes
#   each writer's sequence from the DocumentDB workload collection and
#   reads/writes progress to the longhaul-report ConfigMap (no PVC).
#
# Strategy: Recreate (single-replica stateful driver — never run two
# driver pods concurrently against the same DocumentDB cluster /
# workload collection).
#
# Failure semantics: on critical failure (data loss, policy violation)
# the driver exits non-zero. The Deployment auto-restarts the pod, which
# gives MTBF data; the alert workflow polls the report ConfigMap and
# pages on incident-count thresholds.
#
# Image refs are templated; the longhaul-deploy workflow substitutes:
#   __OWNER__      -> lowercased ${{ github.repository_owner }}
#   __IMAGE_TAG__  -> input.image_tag (e.g. sha-5c30d7b or main)
apiVersion: v1
kind: ConfigMap
metadata:
  name: longhaul-test-config
  namespace: documentdb-test-ns
  labels:
    app.kubernetes.io/name: longhaul-test
    app.kubernetes.io/component: testing
data:
  # Test duration. "0s" = run-until-failure (the design default for the
  # long-haul cluster). Override to a finite value (e.g. "30m", "3h") only
  # for short ad-hoc / smoke runs.
  LONGHAUL_MAX_DURATION: "0s"
  # Target DocumentDB cluster.
  LONGHAUL_NAMESPACE: "documentdb-test-ns"
  LONGHAUL_CLUSTER_NAME: "documentdb-cluster"
  # Writer/verifier counts.
  LONGHAUL_NUM_WRITERS: "5"
  # Operation scheduling.
  LONGHAUL_OP_COOLDOWN: "10m"
  LONGHAUL_RECOVERY_TIMEOUT: "5m"
  # How long the cluster must be observed healthy before the next
  # operation fires. Matches the in-code default (60s) and the design doc
  # (Section "Steady-state gate"). Keep explicit so operators can lower
  # it for chaos runs or raise it during incident triage without a
  # rebuild.
  LONGHAUL_STEADY_STATE_WAIT: "60s"
  # Checkpoint report cadence. Matches the monitor workflow cron (hourly)
  # — bump both together if changed.
  LONGHAUL_REPORT_INTERVAL: "1h"
  # Whether to drop the workload collection on startup. MUST stay "false"
  # on the long-haul cluster — restarts of the driver pod (rolling image
  # updates, node drains) need to resume durability state from the existing
  # DocumentDB workload collection and longhaul-report ConfigMap checkpoints.
  # Override to "true" only for local/dev iterations.
  LONGHAUL_RESET_DATA: "false"
  # Retention: keep only the most recent N documents per writer so the endurance
  # write load does not grow the PVC without bound (a fixed disk always fills
  # eventually otherwise). Pruning removes only already-verified history, so it
  # never affects the durability verdict. At ~10 writes/sec/writer, 2,000,000
  # docs is roughly 55h of per-writer history. Set to "0" to disable pruning.
  LONGHAUL_RETAIN_PER_WRITER: "2000000"
  # Replica range for scale operations.
  # Bounds for spec.instancesPerNode (CRD allows 1-3). Default Min=1, Max=3
  # exercises full scale-up/down cycle. Set Min=Max to disable scale ops.
  LONGHAUL_MIN_INSTANCES: "1"
  LONGHAUL_MAX_INSTANCES: "3"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: longhaul-test
  namespace: documentdb-test-ns
  labels:
    app.kubernetes.io/name: longhaul-test
    app.kubernetes.io/component: testing
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: longhaul-test
      app.kubernetes.io/component: testing
  template:
    metadata:
      labels:
        app.kubernetes.io/name: longhaul-test
        app.kubernetes.io/component: testing
    spec:
      serviceAccountName: longhaul-test
      restartPolicy: Always
      containers:
        - name: driver
          image: ghcr.io/__OWNER__/documentdb-kubernetes-operator/longhaul-test:__IMAGE_TAG__
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: longhaul-test-config
          env:
            - name: LONGHAUL_DOCUMENTDB_URI
              valueFrom:
                secretKeyRef:
                  name: longhaul-documentdb-credentials
                  key: uri
            # Soft heap ceiling below the cgroup limit so Go's GC turns
            # aggressive before the hard limit, preventing OOMKills under a
            # transient allocation spike. The verify path streams its cursor
            # (O(1) memory), so steady-state usage is a few MB; this is a
            # belt-and-suspenders guard, not a working-set requirement.
            - name: GOMEMLIMIT
              value: "200MiB"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
